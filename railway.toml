[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && cd frontend && npm ci && npm run build && cd .. && cp -r frontend/dist public_react"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
